# --- Núcleo do pipeline (roda localmente) ---
langchain==1.4.0
langchain-community==0.4.2
langgraph==1.2.11
chromadb==1.5.9
structlog==26.1.0
pydantic==2.13.5
pydantic-settings==2.15.0
SQLAlchemy==2.0.52

# --- Anonimização (opcional, usada pelo backend Presidio) ---
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
# Após instalar, baixe o modelo do spaCy:
#   python -m spacy download pt_core_news_lg
# (ou en_core_web_lg, se o dataset estiver em inglês)

# --- Embeddings reais para produção (substitui o HashEmbeddings de dev/teste) ---
sentence-transformers==3.3.1

# --- Serving do LLM fine-tunado ---
ollama==0.4.7          # cliente para servir o modelo localmente via Ollama
huggingface-hub==0.27.1

# --- Testes e qualidade ---
pytest==9.1.1
pytest-cov==6.0.0

# --- Fine-tuning (usado APENAS no notebook do Google Colab, não localmente) ---
# unsloth
# bitsandbytes
# transformers
# trl
# peft
# accelerate
# datasets
